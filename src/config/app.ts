// 应用全局配置
export const APP_CONFIG = {
  /**
   * 开发调试模式 (DEV_MODE)
   *
   * true:  显示 Performance 性能监控面板（用于压测、性能调优）
   * false: 普通用户模式，隐藏 Performance 面板
   *
   * 需要调试性能时改为 true 即可，无需改动其他代码。
   */
  DEV_MODE: false,
}
